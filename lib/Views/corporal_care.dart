import 'package:flutter/material.dart';

class CorporalCare extends StatelessWidget {
  const CorporalCare({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const Column(
        children: [
          Text('Corporal Care'),
        ],
      ),
    );
  }
}